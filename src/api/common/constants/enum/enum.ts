export enum LocationType {
  NONE = 'none',
  REGION = 'region',
  AREA = 'area',
  NEAR = 'near',
}

export enum RecommendationType {
  DATE = 'date',
  FRIENDLY = 'friendly',
  GROUP = 'group',
  GOAT = 'goat',
}
